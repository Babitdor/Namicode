Author **NOVA.md** — the onboarding guide an AI coding agent (and humans) read
before working in this repository. This is a real authoring task: produce a
**rich, detailed, accurate** document, and **write it to `{{ nova_md_path }}`**
using your `write_file` tool. Do not just reply with the content — the file is
the deliverable.

## Ground yourself first (use your tools)
A knowledge/architecture graph of this project already exists. Before writing:
- Use `query_project_graph(query="…")` to explore modules, central hubs, and
  cross-module connections (try the central hubs and key files listed in FACTS).
- `read_file` the most important files (entry points, central hubs, config) to
  confirm what they actually do — don't guess. Use **workspace-relative paths**
  (forward slashes); never an absolute or Windows path (`B:\…`) — the filesystem
  rejects those.
- Everything you write MUST be grounded in the graph + the files + the FACTS
  below. Do not invent commands, files, APIs, or conventions.

## Tool rules (important)
- Use ONLY `query_project_graph`, `read_file`, and `write_file`.
- Do **NOT** run shell commands — no `execute`, no `shell`, no `wc`/`ls`/`cat`.
  You do not need to verify the file with the shell; a successful `write_file`
  is enough. Shell commands pause this automated run for approval and stall it.

## Write a thorough NOVA.md (`{{ nova_md_path }}`)
Aim for genuinely useful depth (well-structured, not padded; under
~{{ max_chars }} characters). Include, in this order:

1. `# NOVA.md` + a crisp 2–4 sentence overview of what the project is and its shape.
2. `## Architecture` — narrate how the modules/communities fit together, where the
   important logic lives, and the main data/▷control flows. Reference real files.
3. `## Project Graph` — explain the graph at `.nova/project-graph.json` (+
   `.nova/project-graph.html`), that a summary auto-loads into context each
   session, and how to use `query_project_graph(query="…")` (give 2 concrete
   example queries with real names). Call out the **central hubs** (high blast
   radius — check before editing) and **cross-module connections** (seams). Note
   refresh via `/init` / `/init --update`.
4. `## Module Guide` — a short subsection per major community/module: its purpose,
   key files, and how it connects to the rest. This is the most valuable part —
   be specific and concrete.
5. `## Commands` — build/test/lint/run (from FACTS; verify against config files).
6. `## Code Conventions` — real conventions you can confirm (style, patterns).
7. `## Key Files` — the files that matter most, each with one line on its role.
8. `## Development Workflow` — practical steps (tests before commit, etc.).
9. `## Gotchas` — non-obvious pitfalls you noticed while reading the code (omit if none).

End with a one-line italic note that it was generated by Nova /init.

## FACTS (graph-derived ground truth — do not contradict)
```
{{ facts }}
```

When done, confirm in one sentence that you wrote `{{ nova_md_path }}`.
