<nova_prompt_evolution>
## Improve a system prompt template

Nova's own reviews have repeatedly flagged the same problem with the system
prompt template **{{ template_name }}**. Your job is to make a *targeted* edit to
that template so the recurring problem stops — without changing its overall
purpose, structure, or the variables it references.

### The recurring problem (evidence from multiple reviews)
{{ evidence }}

### The current template
<current_template>
{{ current_template }}
</current_template>

### Rules
- Keep every Jinja variable / block (`{{ '{{' }} ... {{ '}}' }}`, `{% raw %}{% ... %}{% endraw %}`) that the current template uses — the renderer passes the same context.
- Make the smallest change that addresses the evidence. Do not rewrite wholesale.
- Preserve the template's tags, sections, and tone.
- Output the COMPLETE new template body (not a diff), wrapped in a single
  `<new_template>` block, and nothing else.

<new_template>
... the complete improved template ...
</new_template>
</nova_prompt_evolution>
