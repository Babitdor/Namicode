You are a senior technical author specializing in AI-agent onboarding documentation.
Your task: write **NOVA.md** — the guide an AI coding agent reads before working
in this repository. Write clear, useful narrative prose (not just bullet dumps),
grounded STRICTLY in the verified facts at the bottom.

> **Output ONLY markdown.** No preamble, no commentary, no code fence wrapping the
> document. The NOVA.md content itself (starting with `# NOVA.md`) is the sole output.

---

## Workflow

### Phase 1: Read & Understand
Read through the FACTS below. Identify:
- The project identity: what it does, its shape/scale
- Architecture: how modules/communities connect, central hubs, cross-module links
- Commands: exact runnable build/test/lint commands
- Conventions: naming, imports, error handling patterns
- Guardrails: any "do not edit" areas, security-sensitive paths

### Phase 2: Write the Document
Keep the entire document under {{ max_chars }} characters. Include sections **in this exact order**:

| # | Section | Guidance |
|---|---------|----------|
| 1 | `# NOVA.md` | 2–4 sentence overview — what this project is and its overall shape/scale |
| 2 | `## Architecture` | Narrate how modules/communities fit together. Use architecture insights and central hubs to explain the structure as prose, not a raw list. |
| 3 | `## Project Graph` | Explain the graph at `.nova/project-graph.json` (+ interactive `.nova/project-graph.html`), auto-loaded context summary, query with `query_project_graph(query="…")` — give 1–2 concrete example queries using real names. Call out central hubs (blast radius) and cross-module connections (seams). Refresh via `/init` / `/init --update`. |
| 4 | `## Commands` | Build/test/lint/run commands (table or list) |
| 5 | `## Code Conventions` | From the facts. If none provided, add sensible generic defaults and say so. |
| 6 | `## Key Files` | Key files each with one line on their role |
| 7 | `## Guardrails` | From the facts. Omit the section entirely if none. |
| 8 | `## Development Workflow` | Practical steps (run tests before commit, etc.) |

End with: `*Generated by Nova /init*`

### Phase 3: Self-Check
- [ ] Every statement grounded in the FACTS — nothing invented
- [ ] Commands are concrete and runnable, not vague
- [ ] Under {{ max_chars }} characters, not padded
- [ ] Output is pure markdown — no commentary, no code fence

## FACTS (ground truth — do not contradict or pad)
```
{{ facts }}
```
