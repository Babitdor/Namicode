You are writing **NOVA.md** — the onboarding guide an AI coding agent reads
before working in this repository. Write clear, useful narrative prose (not just
bullet dumps), grounded STRICTLY in the verified facts at the bottom.

## Rules
- Ground every statement in the FACTS. **Do not invent** commands, files, APIs,
  modules, or conventions that are not in the facts. If something isn't known,
  omit it rather than guessing.
- Be concrete and concise. Keep the whole document under {{ max_chars }} characters.
- Output **only** the NOVA.md markdown — no preamble, no commentary, and do NOT
  wrap the entire document in a code fence.

## Required sections, in this order
1. `# NOVA.md` — then a 2–4 sentence overview of what this project is and its
   overall shape/scale.
2. `## Architecture` — narrate how the modules/communities fit together and where
   the important logic lives. Use the architecture insights and central hubs to
   explain the structure as prose, not a raw list.
3. `## Project Graph` — explain that Nova built a dependency/architecture graph at
   `.nova/project-graph.json` (with an interactive view at
   `.nova/project-graph.html`), that a summary is auto-loaded into the agent's
   context every session, and how to query it with the
   `query_project_graph(query="…")` tool (give 1–2 concrete example queries using
   real names from the facts). Call out the **central hubs** (high blast radius —
   check dependents before editing) and **cross-module connections** (architectural
   seams). Note that it refreshes with `/init` or `/init --update`.
4. `## Commands` — the build/test/lint/run commands (table or list).
5. `## Code Conventions` — from the conventions facts (add sensible, clearly-generic
   defaults only if none are provided, and say so).
6. `## Key Files` — the key files and what each is for.
7. `## Guardrails` — from the guardrails facts (omit the section if there are none).
8. `## Development Workflow` — a few practical steps (e.g. run tests before commit).

End with a single italic line noting it was generated by Nova /init.

## FACTS (ground truth — do not contradict or pad)
```
{{ facts }}
```
