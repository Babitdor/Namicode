## Iteration {{ iteration_display }}

### Context
Previous work is in the filesystem. Continue making progress.
{% if conversation_context %}

### Prior Conversation
```
{{ conversation_context }}
```
{% endif %}

### Progress Log — READ FIRST  (`{{ progress_path }}`)
{% if progress_notes %}
Notes from previous iterations. Read before acting to avoid redoing completed work:
```
{{ progress_notes }}
```
{% else %}
No progress recorded yet — first iteration. You will create `{{ progress_path }}` at the end.
{% endif %}

### Task
{{ task }}

### Objectives
1. Review progress log + filesystem to assess what's done and what remains
2. Execute the next logical step
3. Validate: test, lint, or verify changes where applicable
4. Update progress log at `{{ progress_path }}`

### Progress Tracking
Before finishing, update `{{ progress_path }}` by appending a new entry. Never delete earlier entries. Each entry:
```
## Iteration {{ iteration_display }}
- Implemented: <what you changed — files, key edits>
- Learned: <decisions, gotchas, dead-ends>
- Next: <immediate next step>
```
Use `write_file("{{ progress_path }}", <full updated contents>)`.

### Output
End with: Progress Summary | Current Status (Complete/Incomplete) | Next Steps | Confirmed progress log updated.