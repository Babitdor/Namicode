## Iteration {{ iteration_display }}

### Context
Previous work is in the filesystem. Continue making progress.
{% if conversation_context %}

### Prior Conversation
```
{{ conversation_context }}
```
{% endif %}

### Progress Log — READ FIRST  (`{{ progress_path }}`)
{% if progress_notes %}
Notes from previous iterations. Read before acting to avoid redoing completed work:
```
{{ progress_notes }}
```
{% else %}
No progress recorded yet — first iteration. You will create `{{ progress_path }}` at the end.
{% endif %}

### Checklist — the completion signal  (`{{ checklist_path }}`)
{% if checklist_md %}
This run stops automatically once **every** item below is checked. Keep it current:
check off what you finish, add items you discover.
```
{{ checklist_md }}
```
({{ checklist_summary }})
{% else %}
No checklist yet. **Create `{{ checklist_path }}` now** as a markdown task list that
breaks `Task` into concrete, verifiable steps — the loop ends when all are `[x]`:
```
- [ ] first concrete step
- [ ] second concrete step
```
{% endif %}
{% if verify_summary %}

### Verification result (last iteration)
The configured check (`{{ verify_path }}`) {% if verify_passed %}**passed**{% else %}**FAILED** — fixing this is the priority this iteration{% endif %}:
```
{{ verify_summary }}
```
{% endif %}
{% if no_change_streak and no_change_streak > 0 %}

### ⚠ No progress warning
The previous {{ no_change_streak }} iteration(s) changed **no files**. Either make a
concrete change this iteration, or — if the task is genuinely done — check off the
remaining checklist items. The loop will stop itself if nothing changes again.
{% endif %}

### Task
{{ task }}

### Objectives
1. Review the progress log, checklist, and filesystem to assess what's done and what remains
2. Execute the next logical step toward an unchecked checklist item
3. Validate: test, lint, or verify changes where applicable
4. Update the checklist (`{{ checklist_path }}`) and progress log (`{{ progress_path }}`)

### Progress Tracking
Before finishing, append a new entry to `{{ progress_path }}` (never delete earlier ones):
```
## Iteration {{ iteration_display }}
- Implemented: <what you changed — files, key edits>
- Learned: <decisions, gotchas, dead-ends>
- Next: <immediate next step>
```
Use `write_file` / `edit_file` for both the progress log and the checklist.

### Output
End with: Progress Summary | Current Status (Complete/Incomplete) | Next Steps | Confirmed checklist + progress log updated.
