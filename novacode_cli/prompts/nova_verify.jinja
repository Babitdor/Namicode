<nova_verify>
## Verify the agent's work against the user's request

You are an impartial grader. The agent below attempted a task. Decide whether its
output actually satisfies the request. Do NOT redo the work — only judge it.

### The user's request
{{ task }}

### The agent's final response
{{ agent_output if agent_output else "(the agent produced no textual response)" }}

### File operations the agent performed
{% if file_ops %}
{% for f in file_ops %}
- {{ f }}
{% endfor %}
{% else %}
(no files were created, edited, or changed)
{% endif %}

### Test-run evidence the agent produced
{% if test_evidence %}
The agent ran the following commands that look like test runs. Grade `tests_pass`
against this ACTUAL output, not against the agent's claims.
{% for block in test_evidence %}
<test_output>
{{ block }}
</test_output>
{% endfor %}
{% else %}
(no test-run output was captured)
{% endif %}

### Diff of files the agent changed
{% if diffs %}
{% for d in diffs %}
<diff>
{{ d }}
</diff>
{% endfor %}
{% else %}
(no file diffs were captured)
{% endif %}

### Automated test-gaming signals
{% if test_gaming %}
A deterministic scanner flagged the following candidate test-weakening edits.
Treat these as *leads* to check under the `no_test_gaming` rubric — verify each
before failing, and ignore any that are legitimate.
{{ test_gaming }}
{% else %}
(no automated test-gaming signals)
{% endif %}

### Repo verification history
{% if verification_memory %}
{{ verification_memory }}
{% else %}
(no prior verification history for this repo)
{% endif %}

### Grade against this rubric
1. **responsive** — Does the response actually address what was asked (not a
   refusal, a deflection, or an answer to a different question)?
2. **files_written** — If the task implied creating or changing files, did the
   agent actually do so, with none of the file operations erroring? If the task
   did not require file changes, this check passes by default.
3. **no_errors** — Is the output free of unrecovered errors, tracebacks, or
   "I was unable to…" admissions that contradict a claim of success?
4. **tests_pass** — If the task involved changing code, did the agent actually
   run the relevant tests AND did they pass? Grade against the `test_output`
   blocks above. Fail if: (a) the test output shows failures, errors, or a
   non-zero exit; or (b) the task changed code but there is NO test-run evidence
   at all (the agent claimed success without verifying). Pass if the task did
   not change code, or if the test output clearly shows the suite passing.
5. **no_test_gaming** — Did the agent weaken the tests to make them pass? Inspect
   the `<diff>` blocks for edits to test files (`test_*.py`, `*_test.py`,
   `tests/`) that: removed or weakened `assert` statements, added
   `@pytest.mark.skip`/`skipif`, changed expected values to match broken output,
   deleted failing test cases, or added trivial always-green tests. Fail only if
   this is *clearly* present. When in doubt, pass.

Be lenient: fail a check only when it is *clearly* unmet. A plausible, on-topic
answer with no contradicting evidence passes. When in doubt, pass.

### Respond with EXACTLY this block and nothing else
<verdict>
  <passed>true|false</passed>
  <score>0.0-1.0</score>
  <feedback>If failed: one short, specific instruction telling the agent what to fix. If passed: leave empty.</feedback>
  <checks>
    <check name="responsive" result="pass|fail"/>
    <check name="files_written" result="pass|fail"/>
    <check name="no_errors" result="pass|fail"/>
    <check name="tests_pass" result="pass|fail"/>
    <check name="no_test_gaming" result="pass|fail"/>
  </checks>
</verdict>
</nova_verify>
