<refine_plan>
## Nova Self-Refinement — propose the smallest evidence-backed improvements

You are planning a surgical self-improvement pass over Nova's own learning state.
Read the trajectory evidence and current state below, then propose the SMALLEST
set of targeted changes that would improve future outcomes. Prefer a `patch` /
`update` to an existing item over creating or deleting anything.

### Current learning state
{% if skills %}
Skills:
{% for s in skills %}
- `{{ s.name }}` — {{ s.description }}
{% endfor %}
{% else %}
Skills: (none)
{% endif %}

{% if prompt_state %}
Prompt overrides:
{% for p in prompt_state %}
- `{{ p.template }}` (active={{ p.has_active }}, candidate={{ p.has_candidate }})
{% endfor %}
{% else %}
Prompt overrides: (none)
{% endif %}

{% if memory_topics %}
Memory topics:
{% for t in memory_topics %}
- `{{ t }}`
{% endfor %}
{% else %}
Memory topics: (none)
{% endif %}

### Trajectory evidence
{{ evidence }}

### Rules
- Emit at most {{ max_items }} `<item>` blocks (one per domain max).
- Every item MUST cite the evidence that motivates it. No speculative changes.
- Prefer the smallest edit: `patch`/`update` over `create`/`delete`.
- Only target user/project skills, the packaged prompt templates listed in
  `prompt_state`, and memory topics listed in `memory_topics`. Never invent a
  template or topic name.
- Memory edits are APPEND-ONLY: `<change>` is a bullet list (one per line) of
  durable facts/lessons to record under the target topic. Never propose deleting
  or rewriting existing memory content.
- If nothing is worth changing, emit `<refine_plan></refine_plan>` (empty).

### Output format
<refine_plan>
  <item>
    <domain>skill|prompt|memory</domain>
    <target>skill-name|template-name|memory-topic</target>
    <action>patch|update|create|delete</action>
    <reason>evidence-backed rationale</reason>
    <change>the minimal proposed edit (for skill: the full improved SKILL.md body
    with YAML frontmatter; for prompt: the full improved template body; for
    memory: a bullet list of lessons to record)</change>
  </item>
</refine_plan>
</refine_plan>
