<nova_evolution>
## Self-Evolution — capture the skill from this completed task

A complex task just finished (complexity signals:
substantive={{ complexity.substantive }}, distinct_tools={{ complexity.distinct_tools }},
subagents={{ complexity.subagents }}, todos={{ complexity.todos_completed }},
recovered_from_error={{ complexity.recovered }}). Distil what was accomplished into a
**reusable skill** so a future similar task is faster and more reliable.

{% if skill_library %}
### Existing skills (the current library)
{% for s in skill_library %}
- `{{ s.name }}` — {{ s.description }}
{% endfor %}
{% else %}
### Existing skills
(none yet)
{% endif %}

### Decide — emit EXACTLY ONE block

1. If this task is a **genuinely new** reusable workflow not covered above, emit a
   new skill:

   <skill>
   <name>kebab-case-name</name>
   <description>Use when … (the trigger — when a future task should reach for this).</description>
   <body>
   ## When to Use
   The concrete situations this applies to (mirror the description's triggers).
   ## Quick Reference
   The key command(s) / one-liner(s), copy-pasteable.
   ## Procedure
   Concrete, ordered steps. Reference real files/commands from THIS task.
   ## Pitfalls
   The gotchas/edge cases you hit and how to avoid them.
   ## Verification
   How to confirm the work is correct (the check/test you ran).
   </body>
   </skill>

2. If this task closely matches one of the existing skills above, **level it up**
   instead of duplicating it — return the COMPLETE improved `SKILL.md` (YAML
   frontmatter + body), folding in what you learned this time:

   <level_up skill="existing-skill-name">
   <body>
   ---
   name: existing-skill-name
   description: "…"
   version: 1.0.1
   tags: [tag-one, tag-two]
   ---

   ## When to Use
   …
   ## Quick Reference
   …
   ## Procedure
   … full improved steps …
   ## Pitfalls
   …
   ## Verification
   …
   </body>
   </level_up>

Rules:
- Emit at most ONE block. Prefer `<level_up>` when a close match exists; otherwise `<skill>`.
- Do NOT capture generic read→edit→test activity with no reusable shape.
- The skill must be specific enough to act on, general enough to reuse.
- Output ONLY the block — no commentary around it.
</nova_evolution>
