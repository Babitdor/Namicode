schema_version = "1.1"

[task]
name = "nova/git-bisect-regression"
description = "Use git history to find the commit that introduced a regression, then fix the bug."
keywords = ["git", "debugging", "bisect", "python"]

[metadata]
difficulty = "medium"
category = "git"

[verifier]
timeout_sec = 1200.0

[agent]
timeout_sec = 1200.0

[environment]
build_timeout_sec = 600.0
cpus = 1
memory_mb = 2048
storage_mb = 10240
gpus = 0
allow_internet = true
