schema_version = "1.1"

[task]
name = "nova/fix-broken-python-project"
description = "Fix multiple bugs in a small Python package so it imports and all tests pass."
keywords = ["python", "debugging", "pytest"]

[metadata]
difficulty = "medium"
category = "debugging"

[verifier]
timeout_sec = 1200.0

[agent]
timeout_sec = 1200.0

[environment]
build_timeout_sec = 600.0
cpus = 1
memory_mb = 2048
storage_mb = 10240
gpus = 0
allow_internet = true
